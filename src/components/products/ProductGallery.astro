---
import type { Product } from '@types'
import ImageThumbnails from './ImageThumbnails.astro'

interface Props {
  product: Product
}

const { product } = Astro.props
---

<div
  class="flex-1 gap-3 min-h-0 flex flex-col
    md:portrait:gap-4
    phone-portrait:max-h-[40%]
    phone-landscape:flex-none
    phone-landscape:basis-[40%]
    phone-landscape:h-full
    tablet-landscape:flex-none
    tablet-landscape:basis-[50%]"
>
  <div class="bg-[#d9d9d9] flex-1 min-h-0">
    <img
      src={product.image}
      alt={product.name}
      data-product-image
      data-default-image={product.image}
      class="size-full object-contain"
      loading="lazy"
    />
  </div>
  <ImageThumbnails product={product} />
</div>
