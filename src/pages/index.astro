---
import { getLocaleRedirectUrl, setLocaleRedirectHeaders } from '@utils/i18'

setLocaleRedirectHeaders(Astro.response.headers)

return Astro.redirect(
  getLocaleRedirectUrl(Astro.request, Astro.url).toString(),
  307,
)
---
